<?xml version="1.0" encoding="utf-8"?>
<androidx.appcompat.widget.Toolbar xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/toolbar_action"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:layout_gravity="center_vertical">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="?attr/selectableItemBackground"
        android:id="@+id/selectorParentView"
        android:orientation="vertical">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/insurance_address_select_label_select_state"
            android:textColor="@color/textColorSecondary"
            android:textAppearance="@style/DearOTextAppearance.Caption" />


        <TextView
            android:layout_width="wrap_content"
            tools:text="Maharashtra"
            android:id="@+id/selectedStateView"
            android:textColor="@color/textColorPrimary"
            android:textAppearance="@style/DearOTextAppearance.Body.Regular.B1"
            android:layout_height="wrap_content" />

        <View
            android:layout_width="match_parent"
            android:background="@color/backgroundGrey"
            android:layout_marginEnd="10dp"
            android:layout_height="2dp"/>

    </LinearLayout>
</androidx.appcompat.widget.Toolbar>
